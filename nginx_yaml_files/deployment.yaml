---
apiVersion: apps/v1
kin:d: Deployment
metadata:
   labels:
      app: nginx-deployment
   namespace: default
spec:
   replicas: 1
   selector:
      matchLabels:
         app: nginx
   template:
      metadata:
         labels:
            app: nginx
      spec:
         containers:
         - image: nginx
           name: nginx
